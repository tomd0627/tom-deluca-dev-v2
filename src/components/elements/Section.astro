---
export interface Props {
  title: string;
  cssClass: string;
  id?: string;
}

const { title, cssClass, id = title } = Astro.props;
---

<section id={id} class={`section ${cssClass}`}>
  <div class="container">
    <slot />
  </div>
</section>
