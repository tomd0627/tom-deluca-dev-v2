---
export interface Props {
  id: string;
  name: string;
  label: string;
  isRequired?: boolean;
  minLength?: number;
}

const { id, name, label, isRequired, minLength } = Astro.props;

let required = false;

if (isRequired === true) {
  required = true;
}
---

<label class="form-field animate-fade">
  <textarea
    id={id}
    name={name}
    placeholder=`${label}`
    class="form-input form-input--textarea"
    required={required}
    minlength={minLength}></textarea>
  <span class="form-line"></span>
  <span class="form-label">
    {label}
    {
      isRequired && (
        <>
          {' '}
          <span class="form-required">*</span>
        </>
      )
    }
  </span>
  <span id={`${id}-error`} class="form-error" role="alert"></span>
</label>
